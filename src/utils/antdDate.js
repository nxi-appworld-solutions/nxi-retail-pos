// const handleDateChange = (date) => {
//   setSelectedDate(date);
//   setFormData((prev) => ({
//     ...prev,
//     date: date ? dayjs(date).format("DD-MMM-YYYY") : "", // backend format
//   }));
// };
